---
name: 文献综述表格填写工作流
description: 批量阅读PDF论文并提取结构化信息填入Excel的标准流程，经NCCL项目验证高质量
type: feedback
originSessionId: dba93759-0138-4ab9-b1ec-dc496cd7994a
---
## 规则：批量文献提取填表工作流（已验证）

当用户要求"阅读N篇论文/文献，将信息填入表格"时，严格按以下流程执行：

### 第一步：理解表格结构
- 先用 openpyxl/pandas 读取Excel，确认标题行、列名、待填行数
- 注意编码：用 `sys.stdout.reconfigure(encoding='utf-8')` 避免乱码

### 第二步：批量提取PDF文本
- 用 pdfplumber 提取每篇PDF的**前5页**（标题、摘要、引言、方法开头）
- 同时提取**末尾3页**（结果、讨论、结论、局限性）
- 分别保存为JSON（key=文件名，value=文本），方便后续Agent读取
- 打印每篇的页数和提取字符数，确认全部成功

### 第三步：并行Agent分析
- 将论文分组，每组5-6篇，派出多个Agent并行处理
- 每个Agent读取两个JSON文件（前段文本 + 末尾文本）
- 要求Agent按严格格式输出：`===PAPER_START===` / `===PAPER_END===` 分隔每篇
- 每篇提取6项：文献题目、发表时间、研究目的、核心发现、创新性、局限性
- 所有内容用中文，未提及则标注"文中未明确提及"，禁止编造

### 第四步：填写Excel
- 将所有Agent结果汇总，用 openpyxl 逐行逐列写入
- 设置自动换行（wrap_text=True）、顶部对齐（vertical='top'）
- 调整列宽、标题行加粗居中
- 保存时注意：如果原文件被占用（PermissionError），自动换文件名保存（如 xxx_filled.xlsx）

### 第五步：验证与清理
- 验证所有行是否6/6列完整填写
- 清理临时文件（提取JSON、中间脚本）

### Why: NCCL项目（17篇论文）执行此流程，用户评价"完成质量非常高"。并行Agent大幅提速，结构化输出保证格式统一，前后文本分开提取确保信息完整。
### How to apply: 任何"批量阅读文献→填表"任务自动启用此流程，无需用户再次说明。适用于PDF论文、专利文件、技术报告等批量文档处理。
